{
	"name": "@maddsua/reveal",
	"version": "3.0.0",
	"description": "Transition library. Sort of.",
	"main": "./dist/esm.js",
	"scripts": {
		"build": "tsc",
		"test:build": "esbuild test/browser.ts --outfile=test/browser.js --bundle --minify"
	},
	"devDependencies": {
		"esbuild": "^0.18.6",
		"typescript": "^5.1.3"
	},
	"files": [
		"dist",
		"package.json"
	]
}
