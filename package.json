{
	"name": "@maddsua/reveal",
	"version": "3.0.0",
	"description": "Transition library. Sort of.",
	"main": "./dist/esm.js",
	"scripts": {
		"build": "tsc",
		"test:build": "esbuild src/index.ts --outfile=browser.js --bundle --minify --outfile=dist/esm.js"
	},
	"devDependencies": {
		"esbuild": "^0.18.6",
		"typescript": "^5.1.3"
	}
}
